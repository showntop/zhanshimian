.purchase-page { padding-left: 28rpx; padding-right: 28rpx; }
.tool-intro { padding: 12rpx 6rpx 26rpx; display: flex; flex-direction: column; gap: 9rpx; }
.intro-title { font-size: 33rpx; font-weight: 700; line-height: 1.4; }
.intro-note { color: var(--ink-secondary); font-size: 22rpx; line-height: 1.55; }
.purchase-layout { padding: 20rpx; border: 1rpx solid var(--line); border-radius: var(--radius-xl); background: var(--surface); }
.product-shot { position: relative; width: 100%; height: 500rpx; margin: 0; padding: 0; overflow: hidden; border-radius: var(--radius-md); background: #f0f0ec; color: var(--ink-secondary); }
.product-shot > image { width: 100%; height: 100%; }
.product-shot > view { height: 100%; display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 13rpx; }
.upload-icon { width: 72rpx; height: 72rpx; }
.upload-icon image { width: 100%; height: 100%; display: block; }
.product-shot > view > text:nth-child(2) { color: var(--ink); font-size: 27rpx; font-weight: 650; }
.product-shot > view > text:last-child { color: var(--ink-tertiary); font-size: 20rpx; }
.replace-chip { position: absolute; right: 16rpx; top: 16rpx; padding: 8rpx 13rpx; border-radius: 999rpx; background: rgba(255,255,255,.9); color: var(--ink); font-size: 19rpx; }
.product-context { padding: 22rpx 2rpx 2rpx; }
.product-context > text { font-size: 24rpx; font-weight: 650; }
.product-context > view { margin-top: 15rpx; display: flex; gap: 9rpx; }
.product-context button { min-width: 104rpx; min-height: 60rpx; margin: 0; padding: 0 18rpx; border-radius: 999rpx; background: #f1f0eb; color: var(--ink-secondary); font-size: 20rpx; line-height: 60rpx; }
.product-context button.active { background: var(--moss); color: #fff; }
.demo-action { width: 100%; min-height: 72rpx; margin-top: 10rpx; background: transparent; color: var(--ink); font-size: 22rpx; }
.purchase-action { margin-top: 14rpx; }
.tool-error-copy { display: block; margin: 12rpx 4rpx 0; color: #9a5d50; font-size: 20rpx; text-align: center; }
.purchase-result { margin-top: 24rpx; padding: 24rpx; border: 1rpx solid var(--line); border-radius: var(--radius-xl); background: var(--surface); }
.result-summary { display: grid; grid-template-columns: 66rpx 1fr; align-items: center; gap: 14rpx; }
.result-mark { width: 66rpx; height: 66rpx; display: flex; align-items: center; justify-content: center; border-radius: 50%; background: var(--moss); color: #fff; font-size: 32rpx; }
.result-summary > view:last-child { display: flex; flex-direction: column; gap: 6rpx; }
.result-summary text:first-child { font-size: 27rpx; font-weight: 700; }
.result-summary text:last-child { color: var(--ink-tertiary); font-size: 20rpx; }
.result-list { margin-top: 22rpx; border-top: 1rpx solid var(--line); }
.result-list > view { min-height: 82rpx; padding: 17rpx 0; display: grid; grid-template-columns: 62rpx 1fr; align-items: center; gap: 12rpx; border-bottom: 1rpx solid var(--line); color: var(--ink-secondary); font-size: 21rpx; line-height: 1.45; }
.result-list > view > text:first-child { text-align: center; font-size: 18rpx; font-weight: 650; }
.positive { color: var(--moss); }.caution { color: #9a7048; }
.closet-match { margin-top: 20rpx; }
.closet-match > text { font-size: 23rpx; font-weight: 650; }
.closet-match > view { margin-top: 13rpx; display: flex; flex-wrap: wrap; gap: 8rpx; }
.closet-match > view text { padding: 8rpx 12rpx; border-radius: 999rpx; background: #efeee9; color: var(--ink-secondary); font-size: 18rpx; }
.result-reason { display: block; margin-top: 18rpx; color: var(--ink-secondary); font-size: 20rpx; line-height: 1.6; }
.save-product { width: 100%; min-height: 76rpx; margin-top: 22rpx; border-radius: var(--radius-md); background: var(--moss); color: #fff; font-size: 23rpx; }
