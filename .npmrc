strict-peer-dependencies=false
auto-install-peers=true
shared-workspace-lockfile=true
# 保持 pnpm 默认隔离链接（杜绝幻影依赖）；Expo/Metro 的解析问题在 apps/mobile/metro.config.js 解决。
# 逃生舱（仅当 Metro 无法配置时启用）：node-linker=hoisted
