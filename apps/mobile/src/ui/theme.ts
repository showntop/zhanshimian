import { theme } from '@zsm/design'
import { spring, sheetIn, sheetOut } from '@zsm/design'

export { theme, spring, sheetIn, sheetOut }

export const colors = theme.color
export const space = theme.space
export const radius = theme.radius
